{
  "extends": [
    "next/core-web-vitals",
    "plugin:jsx-a11y/recommended"
  ],
  "rules": {
    "@next/next/no-img-element": "off",
    "@next/next/no-before-interactive-script-outside-document": "off",
    "react/no-unescaped-entities": "off",
    "react-hooks/exhaustive-deps": "warn",
    "no-restricted-syntax": [
      "error",
      {
        "selector": "MemberExpression[object.object.name='process'][object.property.name='env'][property.name='NEXT_PUBLIC_SITE_URL']",
        "message": "NEXT_PUBLIC_SITE_URL non esiste in questo progetto: il punto unico per l'indirizzo del sito e env.appUrl() in lib/env.ts. Una seconda variabile per la stessa cosa e il difetto che faceva rispondere 500 alla disiscrizione."
      },
      {
        "selector": "MemberExpression[object.object.name='process'][object.property.name='env'][property.name='SUPABASE_SERVICE_ROLE_KEY']",
        "message": "27/8/2026 (R009) — La chiave di servizio si legge in un posto solo: lib/env.ts, e il client si prende con getAdminSupabase(). Cinque rotte se lo costruivano a mano, e la piu' distruttiva — la cancellazione di un account — leggeva la chiave grezza da process.env: cinque posti da ricordare il giorno in cui la chiave si ruota, e dimenticarne uno vuol dire una rotta che smette di funzionare in silenzio."
      }
    ],
    "jsx-a11y/label-has-associated-control": "warn",
    "jsx-a11y/click-events-have-key-events": "warn",
    "jsx-a11y/no-static-element-interactions": "warn",
    "jsx-a11y/no-noninteractive-element-interactions": "warn",
    "jsx-a11y/no-autofocus": "warn",
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "@/components/ErrorState",
            "message": "#7 — Di ErrorState ne esiste uno solo: @/components/ui/ErrorState. Prima ce n'erano due, con nomi di proprieta' diversi (retry vs onRetry): importare quello sbagliato compilava e non mostrava il pulsante Riprova, in silenzio."
          },
          {
            "name": "@/lib/notifications",
            "message": "#44 — Le notifiche non si scrivono dal browser: la regola del database lo vieta e l'errore veniva ingoiato. Le scrive il server (trigger o rotta con client di servizio)."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": [
        "**/*.ts",
        "**/*.tsx"
      ],
      "plugins": [
        "@typescript-eslint"
      ],
      "rules": {
        "@typescript-eslint/no-unused-vars": [
          "error",
          {
            "argsIgnorePattern": "^_",
            "varsIgnorePattern": "^_",
            "caughtErrorsIgnorePattern": "^_"
          }
        ]
      }
    }
  ]
}
