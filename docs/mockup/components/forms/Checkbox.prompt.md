Accessible checkbox with a rich label node — terms opt-ins, filters, toggles. Terracotta accent colour.

```jsx
<Checkbox defaultChecked label="Paga alla consegna (contanti al rider)" />
<Checkbox label={<>Accetto i <a href="#">termini</a></>} error="Devi accettare" />
```
